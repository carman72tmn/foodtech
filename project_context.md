# Контекст Проекта и Правила для ИИ-Ассистента (Antigravity)

**ВНИМАНИЕ АССИСТЕНТУ: При начале каждого нового чата или сессии, ты ОБЯЗАН сначала прочитать этот файл, чтобы понять текущее состояние проекта, историю изменений и строгие правила взаимодействия.**

## 🛑 Строгие Правила Взаимодействия

1. **Язык общения**: ВСЕГДА писать, отвечать, комментировать код и вести документацию **СТРОГО НА РУССКОМ ЯЗЫКЕ**.
2. **Анализ контекста**: Перед выполнением новых задач всегда проверяй предыдущие шаги и изменения (описанные ниже).
3. **Ведение истории**: Обновляй этот файл (или файл `task.md` в артефактах) при внесении значимых изменений в архитектуру или функционал.
4. **Тезисный план**: Всегда веди тезисный план действий. Пиши коротко, по делу, без лишней воды.

---

## 📅 История Запросов, Действий и Изменений

### Этап 1: Стабилизация базы и настройки филиалов (Завершено)

- **Проблема**: Ошибка 500 при загрузке страницы заказов в админ-панели из-за рассинхронизации схемы БД.
- **Действие**: Создана миграция Alembic для добавления недостающих полей (`branch_id`, `customer_id`, бонусы, скидки) в таблицу `orders`. Синхронизирована модель Laravel `Order.php`.
- **Изменение**: Страница заказов теперь загружается корректно.
- **Настройки филиалов**: Добавлены поля широты (`latitude`) и долготы (`longitude`) в настройки филиалов в Vue-компоненте и Laravel-контроллере.

### Этап 2: Интеграция заказов из iiko (Завершено базовое ядро)

- **Задача**: Получение и отображение детальной информации о заказах из системы iiko.
- **Бэкенд**:
  - Создан Webhook (`POST /api/v1/orders/webhook/iiko`) для получения обновлений заказов от iiko в реальном времени.
  - Создан эндпоинт ручной синхронизации (`POST /api/v1/orders/sync`) для загрузки заказов за последние 24 часа.
  - Обновлена таблица `orders` (Alembic миграция) для хранения детализации iiko (начисленные бонусы, состав заказа в JSON, данные клиента, время доставки/опоздания). Поле `telegram_user_id` сделано необязательным.
- **Фронтенд (Админ-панель)**:
  - Создан новый компонент `OrderDetailModal.vue` для детального просмотра заказа (состав заказа, карты Yandex/2GIS, тайминги, финансы).
  - На страницу заказов добавлена кнопка "Обновить (23ч) из iiko", которая вызывает апи-метод синхронизации.
  - Строки таблицы заказов сделаны кликабельными для вызова модального окна.
- **Смежные фиксы**:
  - Исправлен баг, при котором Alembic при `autogenerate` удалял стандартные таблицы Laravel (`sessions`, `jobs` и т.д.). В файл `backend/alembic/env.py` добавлена кастомная функция `include_none_laravel_tables`, которая блокирует удаление служебных таблиц.

### Этап 3: Текущие и следующие шаги (В процессе)

- **Тезисы / План действий**:
  1. **Валидация Фронтенда**: Проверить визуальное отображение модального окна `OrderDetailModal.vue` с реальными/тестовыми данными (пользователь проверяет вручную).
  2. **Глобальные переключатели**: Реализовать тумблеры "Временно закрыть ресторан" / "Отключить доставку" на уровне компании/филиала, убедиться, что фронтенд (клиентская часть) реагирует на эти статусы.
  3. **Маппинг данных iiko**: При необходимости расширить маппинг статусов и данных при получении вебхука.
  4. **Рефакторинг**: Исправить мелкие ошибки линтера в фронтенд-файлах (например, `branches/index.vue`).

---

_Документ обновлен: 27 Февраля 2026_
