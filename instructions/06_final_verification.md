# Шаг 6: Финальная проверка системы

Этот документ поможет проверить, что все компоненты FoodTech работают корректно.

## 6.1 Проверка всех сервисов

```bash
# Проверяем статус всех сервисов
sudo systemctl status postgresql
sudo systemctl status nginx
sudo systemctl status foodtech-api
sudo systemctl status foodtech-bot
sudo systemctl status redis-server  # если установлен

# Все должны показывать "active (running)"
```

## 6.2 Проверка Backend API

### Через curl

```bash
# Проверка здоровья API
curl http://localhost:8000/health

# Ожидаемый ответ:
# {"status":"healthy","database":"connected"}

# Проверка документации API (должен вернуть HTML)
curl http://localhost:8000/docs

# Получение списка категорий
curl http://localhost:8000/api/v1/categories/

# Ожидаемый ответ: [] (пустой массив, если меню еще не загружено)
```

### Через браузер

Откройте в браузере:

- http://your-server-ip:8000/docs - Swagger UI документация
- http://your-server-ip:8000/redoc - ReDoc документация

## 6.3 Проверка Admin Panel

### Через браузер

1. Откройте: https://admin.yourdomain.com
2. Должна появиться страница входа панели администратора
3. Войдите с данными администратора
4. Проверьте доступные разделы:
   - Dashboard (главная страница)

## 6.4 Проверка Telegram Bot

### Базовые команды

1. Найдите бота в Telegram по username
2. Отправьте `/start`
   - ✅ Должно появиться приветствие с кнопками
3. Отправьте `/help`
   - ✅ Должна появиться информация о боте
4. Нажмите "🍕 Меню"
   - ✅ Должны появиться категории (или сообщение о пустом меню)
5. Нажмите "🛒 Корзина"
   - ✅ Должно показать пустую корзину
6. Нажмите "📝 Мои заказы"
   - ✅ Должно показать сообщение об отсутствии заказов

### Проверка логов бота

```bash
# Просмотр логов бота
sudo journalctl -u foodtech-bot -n 50

# Не должно быть ошибок при запуске команд
```

## 6.5 Синхронизация с iiko (если настроено)

### Через Admin Panel

1. Войдите в админ-панель
2. Если настроена интеграция - должна быть кнопка "Синхронизировать с iiko"
3. После синхронизации проверьте, что категории и товары появились

### Через API

```bash
# Запрос синхронизации
curl -X POST http://localhost:8000/api/v1/iiko/sync-nomenclature

# Проверка категорий после синхронизации
curl http://localhost:8000/api/v1/categories/

# Проверка товаров
curl http://localhost:8000/api/v1/products/
```

## 6.6 Тестирование полного цикла заказа

### 1. Добавление тестовых данных (если нет iiko)

```bash
# Подключаемся к базе данных
psql -U foodtech_user -d foodtech_db -h localhost

# Добавляем тестовую категорию
INSERT INTO categories (name, description, is_active, created_at, updated_at)
VALUES ('Пицца', 'Вкусная пицца', true, NOW(), NOW());

# Добавляем тестовый товар
INSERT INTO products (name, description, price, category_id, is_available, created_at, updated_at)
VALUES ('Маргарита', 'Классическая пицца', 450.00, 1, true, NOW(), NOW());

# Выходим
\q
```

### 2. Тестирование через Telegram Bot

1. Откройте бота в Telegram
2. Нажмите "🍕 Меню"
3. Выберите категорию "Пицца"
4. Выберите "Маргарита"
5. Нажмите "➕ Добавить в корзину"
6. Вернитесь в главное меню
7. Нажмите "🛒 Корзина"
8. Нажмите "✅ Оформить заказ"
9. Введите тестовые данные:
   - Имя: Тест Тестов
   - Телефон: +79991234567
   - Адрес: ул. Тестовая, д. 1
   - Комментарий: Тестовый заказ
10. ✅ Должно прийти подтверждение заказа

### 3. Проверка заказа в базе данных

```bash
# Подключаемся к БД
psql -U foodtech_user -d foodtech_db -h localhost

# Проверяем заказ
SELECT * FROM orders ORDER BY created_at DESC LIMIT 1;

# Проверяем позиции заказа
SELECT * FROM order_items WHERE order_id = (SELECT id FROM orders ORDER BY created_at DESC LIMIT 1);

# Выходим
\q
```

### 4. Проверка через API

```bash
# Получаем последний заказ
curl http://localhost:8000/api/v1/orders/?limit=1
```

## 6.7 Проверка автозапуска

```bash
# Перезагружаем сервер для проверки автозапуска всех сервисов
sudo reboot

# После перезагрузки подключитесь снова и проверьте:
sudo systemctl status foodtech-api
sudo systemctl status foodtech-bot
sudo systemctl status nginx
sudo systemctl status postgresql

# Все должны быть "active (running)"
```

## 6.8 Проверка логов

```bash
# Backend API
sudo journalctl -u foodtech-api -n 50

# Telegram Bot
sudo journalctl -u foodtech-bot -n 50

# Nginx access log
sudo tail -f /var/log/nginx/access.log

# Nginx error log
sudo tail -f /var/log/nginx/error.log

# PostgreSQL
sudo tail -f /var/log/postgresql/postgresql-15-main.log
```

## 6.9 Мониторинг производительности

```bash
# Проверка использования ресурсов
htop

# Проверка дискового пространства
df -h

# Проверка использования памяти
free -h

# Активные подключения к PostgreSQL
sudo -u postgres psql -c "SELECT count(*) FROM pg_stat_activity;"

# Статус Nginx
sudo nginx -t
```

## 6.10 Резервное копирование

### Создание бэкапа базы данных

```bash
# Создаем директорию для бэкапов
sudo mkdir -p /backup

# Создаем бэкап
sudo -u postgres pg_dump foodtech_db > /backup/foodtech_$(date +%Y%m%d_%H%M%S).sql

# Или в сжатом виде
sudo -u postgres pg_dump foodtech_db | gzip > /backup/foodtech_$(date +%Y%m%d_%H%M%S).sql.gz
```

### Автоматическое резервное копирование

```bash
# Создаем скрипт бэкапа
sudo nano /opt/foodtech/backup.sh
```

**Содержимое скрипта:**

```bash
#!/bin/bash
BACKUP_DIR="/backup"
DATE=$(date +%Y%m%d_%H%M%S)
sudo -u postgres pg_dump foodtech_db | gzip > "$BACKUP_DIR/foodtech_$DATE.sql.gz"
# Удаляем бэкапы старше 7 дней
find $BACKUP_DIR -name "foodtech_*.sql.gz" -mtime +7 -delete
```

```bash
# Делаем скрипт исполняемым
sudo chmod +x /opt/foodtech/backup.sh

# Добавляем в cron (ежедневно в 2:00)
sudo crontab -e
# Добавьте строку:
0 2 * * * /opt/foodtech/backup.sh
```

## 6.11 Чеклист проверки

Отметьте каждый пункт после проверки:

- [ ] PostgreSQL запущен и работает
- [ ] Nginx запущен и работает
- [ ] Backend API запущен и отвечает на запросы
- [ ] Telegram Bot запущен и отвечает на команды
- [ ] Admin Panel доступна через браузер
- [ ] SSL сертификаты установлены и работают
- [ ] База данных содержит необходимые таблицы
- [ ] Можно создать тестовый заказ через бота
- [ ] Заказ сохраняется в базе данных
- [ ] Логи не содержат критических ошибок
- [ ] Все сервисы автоматически запускаются после перезагрузки
- [ ] Настроено резервное копирование

## 6.12 Дальнейшие шаги

После успешной проверки всех компонентов:

1. **Настройте мониторинг** - установите систему мониторинга (например, Prometheus + Grafana)
2. **Настройте оповещения** - настройте алерты о проблемах
3. **Оптимизируйте производительность** - настройте кэширование, индексы БД
4. **Документируйте изменения** - ведите документацию всех настроек
5. **Регулярные обновления** - следите за обновлениями зависимостей

## Поздравляем! 🎉

Система FoodTech успешно развернута и готова к использованию!

### Полезные ссылки

- Backend API Docs: http://your-server-ip:8000/docs
- Admin Panel: https://admin.yourdomain.com
- Telegram Bot: @your_bot_username

### Поддержка

При возникновении проблем:

1. Проверьте логи соответствующего сервиса
2. Обратитесь к документации компонентов
3. Проверьте GitHub Issues: https://github.com/carman72tmn/foodtech/issues
