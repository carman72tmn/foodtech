# Шаг 5: Развертывание Telegram Bot (aiogram)

## 5.1 Создание Telegram бота

Перед развертыванием необходимо создать бота через BotFather:

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "FoodTech Delivery Bot")
4. Введите username бота (должен заканчиваться на `bot`, например: `foodtech_delivery_bot`)
5. Сохраните полученный **токен** - он понадобится далее

## 5.2 Подготовка директории

```bash
# Переходим в директорию бота
cd /opt/foodtech/bot
```

## 5.3 Создание виртуального окружения

```bash
# Создаем виртуальное окружение Python
python3 -m venv venv

# Активируем виртуальное окружение
source venv/bin/activate

# Обновляем pip
pip install --upgrade pip
```

## 5.4 Установка зависимостей

```bash
# Устанавливаем зависимости из requirements.txt
pip install -r requirements.txt

# Проверка установки
pip list | grep aiogram
```

## 5.5 Настройка окружения

```bash
# Копируем файл с примером переменных окружения
cp .env.example .env

# Редактируем .env файл
nano .env
```

**Содержимое .env:**

```ini
# Токен бота (получили от BotFather)
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# URL Backend API (локальный адрес)
API_URL=http://localhost:8000/api/v1

# Режим отладки
DEBUG=False
```

⚠️ **ВАЖНО:** Замените `BOT_TOKEN` на реальный токен, полученный от BotFather!

## 5.6 Тестовый запуск

```bash
# Убедитесь, что виртуальное окружение активно
source venv/bin/activate

# Запускаем бота для проверки
python main.py

# Бот должен запуститься и вывести: "🤖 Бот запущен!"
# Откройте Telegram и найдите своего бота
# Отправьте команду /start - бот должен ответить

# Остановите бота: Ctrl+C
```

## 5.7 Создание systemd сервиса

```bash
# Создаем файл сервиса
sudo nano /etc/systemd/system/foodtech-bot.service
```

**Содержимое файла:**

```ini
[Unit]
Description=FoodTech Telegram Bot
After=network.target foodtech-api.service

[Service]
Type=simple
User=www-data
Group=www-data
WorkingDirectory=/opt/foodtech/bot
Environment="PATH=/opt/foodtech/bot/venv/bin"
ExecStart=/opt/foodtech/bot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Или копируем готовый файл:**

```bash
sudo cp /opt/foodtech/configs/systemd/foodtech-bot.service /etc/systemd/system/
```

## 5.8 Настройка прав доступа

```bash
# Меняем владельца директории на www-data
sudo chown -R www-data:www-data /opt/foodtech/bot

# Делаем .env файл доступным только для владельца
sudo chmod 600 /opt/foodtech/bot/.env
```

## 5.9 Запуск сервиса

```bash
# Перезагружаем systemd
sudo systemctl daemon-reload

# Включаем автозапуск
sudo systemctl enable foodtech-bot

# Запускаем сервис
sudo systemctl start foodtech-bot

# Проверяем статус
sudo systemctl status foodtech-bot

# Должно показать "active (running)"
```

## 5.10 Проверка логов

```bash
# Просмотр логов в реальном времени
sudo journalctl -u foodtech-bot -f

# Последние 50 строк логов
sudo journalctl -u foodtech-bot -n 50

# Логи за последний час
sudo journalctl -u foodtech-bot --since "1 hour ago"
```

## 5.11 Полезные команды

```bash
# Перезапуск бота
sudo systemctl restart foodtech-bot

# Остановка бота
sudo systemctl stop foodtech-bot

# Проверка статуса
sudo systemctl status foodtech-bot
```

## 5.12 Тестирование бота

Откройте Telegram и найдите своего бота по username:

1. Отправьте `/start` - должно появиться приветствие с кнопками меню
2. Нажмите "🍕 Меню" - должны появиться категории (если вы уже синхронизировали меню)
3. Нажмите "🛒 Корзина" - должно показать пустую корзину
4. Нажмите "ℹ️ Помощь" - должна появиться информация о боте

## 5.13 Настройка команд бота (опционально)

Для улучшения UX можно настроить команды через BotFather:

1. Отправьте `/setcommands` в [@BotFather](https://t.me/BotFather)
2. Выберите вашего бота
3. Отправьте список команд:

```
start - Запустить бота
help - Показать помощь
```

## 5.14 Настройка описания бота (опционально)

```
# Через BotFather:
/setdescription - Установить описание бота
/setabouttext - Установить текст "О боте"
/setuserpic - Установить аватар бота
```

## 5.15 Webhook режим (альтернатива polling)

Для продакшена можно использовать webhook вместо polling:

```bash
# Это требует дополнительной настройки Nginx и HTTPS
# Для начала polling режим работает отлично
```

## Устранение проблем

### Бот не отвечает

```bash
# Проверьте логи
sudo journalctl -u foodtech-bot -n 100

# Проверьте, что Backend API работает
curl http://localhost:8000/health

# Проверьте токен бота в .env
cat /opt/foodtech/bot/.env
```

### Ошибка подключения к API

```bash
# Проверьте, что Backend API запущен
sudo systemctl status foodtech-api

# Проверьте API_URL в .env
cat /opt/foodtech/bot/.env | grep API_URL
```

### Ошибки в логах

```bash
# Полные логи с деталями
sudo journalctl -u foodtech-bot --no-pager | tail -n 200
```

## Следующий шаг

Переходите к [06_final_verification.md](06_final_verification.md) для финальной проверки всей системы.
